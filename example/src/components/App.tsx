import '@vkontakte/vkui/dist/vkui.css'
import {FunctionalComponent, h} from 'preact'
import {Router} from '@unexp/router'

export const App: FunctionalComponent = () => {
  return <Router>1 (два)</Router>
}
